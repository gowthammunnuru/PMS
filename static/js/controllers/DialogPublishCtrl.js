// Generated by CoffeeScript 1.8.0
angular.module("perform").controller("DialogPublishCtrl", [
  "$scope", "Auth", "$modal", "$modalInstance", "messages", function($scope, Auth, $modal, $modalInstance, messages) {
    $scope.messages = messages;
    $scope.selfInitials = (_.map(Auth.getUser().cn.split(' '), function(x) {
      return x[0];
    })).join('');
    $scope.ok = function() {
      return $modalInstance.close();
    };
    $scope.cancel = function() {
      return $modalInstance.dismiss('cancel');
    };
    $scope.extra_confirmation = '';
  }
]);
